## Pseudo Code for auto-git-push

### First identify if you are in a repository.

if current_directory has ".git" folder; then
 a    get the status of git 
     track the files that are udated/created
     commit your tracked files with a message
     push them to github (scm)
 else
     ask for the repo name & search tge system for the directory
     if result is found; then 
       go inside the directory
       else
       notify the user repo does not exist
       fi
fi
